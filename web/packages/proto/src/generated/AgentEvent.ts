// DO NOT EDIT — generated by wiregen (`cargo run -p wiregen`; gate: `-- --check`).

import type { DoneStatus } from "./DoneStatus";
import type { HarnessId } from "./HarnessId";
import type { SlashCommand } from "./SlashCommand";
import type { ToolCall } from "./ToolCall";
import type { ToolDiff } from "./ToolDiff";
import type { UserInputQuestion } from "./UserInputQuestion";

/**
 * The normalized streaming event every harness emits.
 *
 * Mirrors zeron's `AgentEvent` tagged enum.
 */
export type AgentEvent = { "type": "sessionStarted", harness: HarnessId, model: string, tools: Array<string>, cwd: string, 
/**
 * Harness-native session id (used for resume).
 */
sessionId: string, assistantMessageId: string, } | { "type": "textDelta", text: string, } | { "type": "generatedImage", id: string, path: string, name: string, mimeType: string, } | { "type": "reasoningDelta", text: string, } | { "type": "assistantMessageCompleted", assistantMessageId: string, } | { "type": "toolCall", id: string, call: ToolCall, } | { "type": "toolResult", id: string, isError: boolean, 
/**
 * Tool output text, capped by the emitting harness (ACP tool-call
 * content; claude/codex adapters never populate it). The doc-side
 * fold applies its own byte cap before anything persists.
 */
output?: string | null, 
/**
 * Inline file diff for edit-shaped tools (ACP `Diff` content).
 */
diff?: ToolDiff | null, } | { "type": "contextUsage", tokens: number | null, window: number | null, } | { "type": "usage", inputTokens: number, outputTokens: number, } | { "type": "availableCommands", commands: Array<SlashCommand>, } | { "type": "error", message: string, } | { "type": "inputRequested", requestId: string, questions: Array<UserInputQuestion>, } | { "type": "inputResolved", requestId: string, } | { "type": "steered", assistantMessageId: string | null, nextAssistantMessageId: string | null, } | { "type": "done", status: DoneStatus, result: string | null, error: string | null, sessionId: string | null, } | { "type": "userMessage", text: string, } | { "type": "subagent", parentToolUseId: string, event: AgentEvent, };
