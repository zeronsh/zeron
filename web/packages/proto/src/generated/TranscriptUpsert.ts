// DO NOT EDIT — generated by wiregen (`cargo run -p wiregen`; gate: `-- --check`).

import type { SessionMessageEntry } from "./SessionMessageEntry";

/**
 * An inserted or replaced entry, positioned after `after` (`None` = head).
 * Anchors are prior upserts of the same frame or unchanged entries, so
 * applying upserts in frame order always finds them settled.
 */
export type TranscriptUpsert = { after: string | null, entry: SessionMessageEntry, };
