// DO NOT EDIT — generated by wiregen (`cargo run -p wiregen`; gate: `-- --check`).

import type { ContextUsage } from "./ContextUsage";
import type { SessionMessageEntry } from "./SessionMessageEntry";
import type { TextAppend } from "./TextAppend";
import type { TranscriptBaseline } from "./TranscriptBaseline";
import type { TranscriptUpsert } from "./TranscriptUpsert";

/**
 * Transcript changes and the current host-owned context snapshot travel together.
 * Older readers ignore `contextUsage`; older hosts decode as unknown usage.
 */
export type TranscriptUpdate = { contextUsage: ContextUsage | null, 
/**
 * Historical content included in this update, independent of reset/delta
 * encoding. Omitted on ordinary live updates and by older engines.
 */
replayBaseline?: TranscriptBaseline | null, } & ({ reset: Array<SessionMessageEntry>, } | { upsert: Array<TranscriptUpsert>, append: Array<TextAppend>, remove: Array<string>, 
/**
 * Expected transcript length after applying this frame — the desync
 * tripwire: a consumer that lands elsewhere resubscribes for a reset.
 */
count: number, });
