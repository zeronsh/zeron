// DO NOT EDIT — generated by wiregen (`cargo run -p wiregen`; gate: `-- --check`).

/**
 * Why a queued row is not currently eligible for automatic or explicit
 * delivery. Kept on the row so moves preserve it and deleting the row cannot
 * leave an orphaned lease behind.
 */
export type QueueDeliveryGate = { "kind": "editing", leaseId: string, ownerDeviceId: string, ownerInstanceId: string, acquiredAtMs: number, expiresAtMs: number, baseTextHash: string, } | { "kind": "reviewRequired", previousLeaseId: string, ownerDeviceId: string, sinceMs: number, baseTextHash: string, };
