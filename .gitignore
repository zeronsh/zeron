/target
**/*.rs.bk
# Local-only scratch space — holds signing/notary credentials, never commit.
/ignore/
*.pdb
.DS_Store
.env
.env.local
node_modules
.wrangler
build/
edge/dist/

# Generated by cargo ndk in CI (native Loro lib) — never commit binaries.
apps/android/app/src/main/jniLibs/

# Machine-local SDK path — never commit.
local.properties
.gradle/
